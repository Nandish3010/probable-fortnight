{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://taal.example/schemas/tools/customer.place_order.schema.json",
  "title": "customer.place_order",
  "description": "Contract for the customer agent tool `place_order`. Tools are deterministic Python or store reads; no LLM inside.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "input",
    "output"
  ],
  "properties": {
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "customer_id",
        "node_id",
        "lines",
        "play_id"
      ],
      "properties": {
        "customer_id": {
          "type": "string"
        },
        "node_id": {
          "type": "string"
        },
        "lines": {
          "type": "array"
        },
        "play_id": {
          "type": "string"
        }
      }
    },
    "output": {
      "type": "object",
      "required": [
        "order_id",
        "total_inr"
      ],
      "properties": {
        "order_id": {
          "type": "string"
        },
        "total_inr": {
          "type": "number"
        },
        "lines": {
          "type": "array",
          "description": "What the order actually contains, including any item a bundle mechanic added silently, so the agent's confirmation can name it.",
          "items": {
            "type": "object",
            "required": [
              "sku",
              "name",
              "qty",
              "price",
              "discount"
            ],
            "properties": {
              "sku": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "qty": {
                "type": "integer"
              },
              "price": {
                "type": "number"
              },
              "discount": {
                "type": "number"
              }
            }
          }
        }
      }
    }
  }
}
